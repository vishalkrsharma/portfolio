import Loader from '@/components/ui/loader';

const LoadingPage = () => {
  return (
    <main className='mx-auto min-h-screen flex items-center justify-center'>
      <Loader />
    </main>
  );
};

export default LoadingPage;
